- provide(:url, "/groups/#{@group.id}/members/")
%h3{style: "margin-bottom: 20px;"}Alla medlemmar i gruppen
%ul{data: {role: 'listview', inset: 'true', filter: 'true', 'filter-placeholder' => 'Sök i listan...'}}
  - @members.each do |member|
    %li
      %a{href: group_member_path(@group, member)}
        #{member.name}
        - if member.is_trainer
          (Tränare)
        - else
          (-#{member.birthyear.strftime("%y")})

- if current_user.admin?
  %a{href: new_group_member_path, data: {role: 'button', icon: 'plus'}}Lägg till medlem